Fedora users

Monitoring plugins for Fedora have all been packaged separately. For
example, to install the check_http just install monitoring-plugins-http.

All plugins are installed in the architecture dependent directory
/usr/lib{,64}/monitoring/plugins/.
